namespace BetFlowSystems.Models
{
    public enum BetStatus
    {
        Pending,
        Win,
        Loss,
        Cancelled,
    }

    public enum DebitOrCredit
    {
        Debit,
        Credit,
    }
    
    public enum AccState {

        Open,
        Closed,

    }
}
